version: "3.9"

services:
  nextjs:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: devxbd
    ports:
      - "8001:3000"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
